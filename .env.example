# SMTP Configuration
SMTP_FROM=your-email@example.com          # The sender email address used for SMTP transactions
SMTP_HOST=smtp.example.com                # The SMTP server host
SMTP_PORT=587                             # The SMTP server port (e.g., 587 for TLS, 465 for SSL)
SMTP_SECURE=false                         # Whether to use TLS/SSL (true or false)
SMTP_USER=your-smtp-username              # Your SMTP username (often your email address)
SMTP_PASS=your-smtp-password              # Your SMTP password

# API Tokens
HFS_TOKEN=your-hfs-token                  # Token for accessing the HFS (custom authentication system)

# Database Connection
DATABASE_URL=mongodb+srv://user:password@cluster.mongodb.net/database-name
                                            # MongoDB connection string or other database URL

# Clerk Authentication
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_abc123
                                            # Your Clerk publishable key for the frontend
CLERK_SECRET_KEY=sk_live_abc123             # Your Clerk secret key for backend operations

# Clerk URLs (optional, for custom sign-in/sign-up redirects)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in      # URL path for sign-in page
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up      # URL path for sign-up page
 